# Activate with: --spring.profiles.active=prod
# Disables the Swagger UI and raw OpenAPI spec so the full API surface
# (paths, DTO field names, validation rules) isn't exposed publicly.
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false
